# Model Zoo

This directory is used to store pretrained models used by the project. You can use the script `download_model.sh` in the current directory to download models in advance, thereby avoiding any network issues when running the code.

File structure:


```text
model_zoo
└── clip_model
    ├── ViT-B-16.pt
    └── ViT-L-14.pt
```
